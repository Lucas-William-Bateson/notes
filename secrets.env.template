# blog secrets — managed via Proton Pass CLI
# Regenerate: pass-cli inject secrets.env.template -o secrets.env

IMAGE_FULL={{ pass://Secrets management/blog/image_full }}
CONTAINER_NAME=blog
APP_NAME=blog
ENVIRONMENT=production
IMAGE_TAG=latest

HOST_PORT=80
APP_PORT=80

CPU_LIMIT=0.5
MEMORY_LIMIT=256M
MIN_CPU_LIMIT=0.1
MIN_MEMORY_LIMIT=64M

NETWORK_NAME=blog-network
